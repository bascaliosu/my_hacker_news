{% block header %}
<div class="container">
    <div class="row">
        <a href="{{ app.url_generator.generate('homepage') }}" >Homepage</a>
        &nbsp;|&nbsp;
        <a href="{{ app.url_generator.generate('newest') }}" >Newest</a>
    </div>
</div>
{% endblock %}
